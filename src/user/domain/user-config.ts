export class UserConfig {
  preparation: {
    shvLinkDisabled: boolean;
    dabsLinkDisabled: boolean;
    links: string[];
  };
  notifications: {
    email: {
      appointment: boolean;
    };
  };
}